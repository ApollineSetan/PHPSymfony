{% extends 'base.html.twig' %}

{% block title %}Comptes{% endblock %}

{% block body %}
<div class="bg-gray-100 py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
            {% for account in accounts %}
            <a href="{{ path('app_accountcontroller_showaccount', {'id': account.id}) }}">
                <div class="bg-white rounded-lg shadow-lg overflow-hidden" id="account-{{ account.id }}">
                    <div class="px-6 py-4">
                        <h2 class="text-xl font-semibold text-gray-800">{{ account.firstname }} {{ account.lastname }}</h2>
                        <p class="text-gray-600 mt-2">{{ account.email }}</p>
                    </div>
                    <div class="px-6 py-4 bg-gray-100 border-t">
                        <p class="text-sm text-gray-500">Rôle : {{ account.roles | join(', ') }}</p>
                    </div>
                </div>
            </a>
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}
